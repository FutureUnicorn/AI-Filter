[project]
name = "ai-filter"
version = "0.1.0"
description = "Employer-side hiring-signal layer: rubric-driven evidence extraction with verbatim source-citation validation."
requires-python = ">=3.13"
dependencies = [
    "openai>=3.3.1",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.uv]
required-version = "0.11.29"
